name: First Job
'on':
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run 1
        run: 'echo Hello'
          test1:
            runs-on: ubuntu-latest
            steps:
              - name: Run 2
                run: 'echo world!'
            test3:
              runs-on: ubuntu-latest
              steps:
                - name: Run 2
                  run: 'echo world!'

